model:
  base_weights: models/yolov8n.pt

data:
  dataset_yaml: ppe_dataset/data.yaml

training:
  epochs: 60
  imgsz: 640
  batch: 8
  device: cpu
  project: runs/detect
  name: infra_ppe_v1_retrain