# InfraGuard Training Configuration
# Stable CPU Production Setup - YOLO11n Upgrade

model:
  base_weights: models/yolo11n.pt

data:
  dataset_yaml: ppe_dataset/data.yaml

training:
  epochs: 30
  imgsz: 416
  batch: 6
  device: cpu
  project: runs/detect
  name: infra_ppe_v2_yolo11n
  workers: 0
  resume: false