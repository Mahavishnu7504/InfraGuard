# InfraGuard Training Configuration
# Safe CPU Setup - YOLOv8s

model:
  base_weights: models/yolov8s.pt

data:
  dataset_yaml: ppe_dataset/data.yaml

training:
  epochs: 50
  imgsz: 416              # Reduced from 512 (big RAM saver)
  batch: 4                # Reduced from 6 (critical fix)
  device: cpu
  project: runs           # FIXED (not runs/detect)
  name: infra_ppe_v2_yolov8s_cpu
  workers: 0
  resume: false