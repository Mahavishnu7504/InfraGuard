# InfraGuard Training Configuration
# Stable CPU Production Setup - YOLOv8n (Clean Restart)

model:
  base_weights: models/yolov8n.pt

data:
  dataset_yaml: ppe_dataset/data.yaml

training:
  epochs: 30
  imgsz: 416
  batch: 6
  device: cpu
  project: runs/detect        # ✅ Correct path
  name: infra_ppe_v1_cpu_clean
  workers: 0
  resume: false